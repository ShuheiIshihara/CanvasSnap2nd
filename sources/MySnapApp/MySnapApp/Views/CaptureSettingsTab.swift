import SwiftUI

struct CaptureSettingsTab: View {
    var body: some View {
        Form {
            Section("権限ステータス") {
                Text("Phase 3/4 で実装します")
                    .foregroundStyle(.secondary)
            }
        }
        .formStyle(.grouped)
    }
}
