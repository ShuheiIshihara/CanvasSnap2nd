import SwiftUI

struct HotkeySettingsTab: View {
    var body: some View {
        Form {
            Section("キャプチャショートカット") {
                Text("Phase 4 で実装します")
                    .foregroundStyle(.secondary)
            }
        }
        .formStyle(.grouped)
    }
}
